"""FST datatype + Penn Treebank transducer construction.

A small self-contained FST/FSA library (relocated from the reference
implementation) plus the PTB FST builder. This is the only place the
experiments use the native FST datatype; everything else is pynini.
"""
